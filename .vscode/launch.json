{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach to Godot C#",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:pickProcess}",
            "justMyCode": false
        }
    ]
}
